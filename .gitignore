__pycache__/
*.egg-info/
outputs/
slurm/logs/
wandb/

# vendored SimplerEnv: keep the code+assets, not our rollout outputs
eval/bridge/SimplerEnv/results/

# Experiment artifacts: extracted features. Only code and the written-up
# results (README.md) belong in the repo, matching the convention used
# throughout eval/bridge/ -- .npz files are regenerable from extract.slurm.
eval/*/experiment*/*.npz
